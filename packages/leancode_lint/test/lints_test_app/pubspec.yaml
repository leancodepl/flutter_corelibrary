name: lints_test_app
description: Flutter app for lint rules testing

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  flutter_hooks: ^0.20.3

dev_dependencies:
  custom_lint: ^0.5.3
  leancode_lint:
    path: ../../../leancode_lint

flutter:
  uses-material-design: true
