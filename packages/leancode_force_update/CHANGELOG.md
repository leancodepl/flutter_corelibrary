## 1.0.4-alpha

- Upgrade `leancode_contracts` to `^0.10.0` and `leancode_contracts_generator` to `^0.19.0`
- Drop the now-unused `json_serializable` dev dependency

## 1.0.3-alpha

- Upgrade `leancode_contracts` dependency to `^0.9.0`
- Upgrade `leancode_contracts_generator` dev dependency to `^0.18.0`
- Upgrade `Leancode.ForceUpdate.Contracts` to `10.0.2860`
- Regenerate contracts

## 1.0.2-alpha

- Upgrade `package_info_plus` dependency to `^10.2.0`

## 1.0.1-alpha

- Fix: don't check for updates when the app is not in the foreground

## 1.0.0-alpha

Initial release
