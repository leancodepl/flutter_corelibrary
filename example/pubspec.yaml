name: example
description: Example app for leancode_markup pacakge.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  leancode_markup:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  leancode_lint: ^7.0.0+1

flutter:
  uses-material-design: true
