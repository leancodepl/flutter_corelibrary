name: example
description: Example app for leancode_markup pacakge.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  leancode_markup:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  leancode_lint: ^4.0.0+1

flutter:
  uses-material-design: true
